# Track B Config 2: DeltaNet block replacement with ESN
# Used by scripts/train_track_b_deltanet.py
#
# Architecture: Frozen Qwen3.5-0.8B-Base + LoRA + ESN replacement of DeltaNet blocks
#   - Replace 6 of 18 DeltaNet blocks (every 3rd) with ESN reservoir modules
#   - Learned gating: output = gate * esn_out + (1-gate) * original_deltanet_out
#   - DEEPER integration than Track A: ESN replaces rather than supplements DeltaNet
#
# Updated with Track A learnings: r=1000, no grad ckpt, memory tasks, gate warmup

# ---------------------------------------------------------------------------
# Model
# ---------------------------------------------------------------------------
model_name: "qwen3.5-0.8b"
dtype: "bfloat16"
device: "cuda"

# ---------------------------------------------------------------------------
# ESN reservoir — Track A best (r=1000 beats r=10000)
# ---------------------------------------------------------------------------
reservoir_size: 1000
spectral_radius: 0.9
leak_rate: 0.5
input_scaling: 1.0
reservoir_sparsity: 0.01
reservoir_seed: 42

# ---------------------------------------------------------------------------
# DeltaNet replacement config
# ---------------------------------------------------------------------------
# Replace every 3rd DeltaNet block -> 6 of 18 DeltaNet blocks replaced
replace_every_nth_deltanet: 3
num_deltanet_blocks: 18          # Qwen3.5-0.8B has 18 DeltaNet blocks
# Gate initialization: start low, use warmup to ramp up
replacement_gate_init: 0.1
gate_warmup_steps: 10
gate_warmup_target: 0.1

# ---------------------------------------------------------------------------
# LoRA adapter settings
# ---------------------------------------------------------------------------
lora_rank: 16
lora_alpha: 32.0
lora_dropout: 0.05
lora_targets:
  - q_proj
  - v_proj

# ---------------------------------------------------------------------------
# Optimizer
# ---------------------------------------------------------------------------
lr: 2.0e-4
interface_lr: 1.0e-3
weight_decay: 0.01

# ---------------------------------------------------------------------------
# LR schedule
# ---------------------------------------------------------------------------
warmup_steps: 100
max_steps: 5000

# ---------------------------------------------------------------------------
# Training loop — Track A: batch=1, grad_accum=16, no gradient checkpointing
# ---------------------------------------------------------------------------
batch_size: 1
grad_accum: 16
max_seq_length: 2048
gradient_checkpointing: false
seed: 42

# ---------------------------------------------------------------------------
# Data: FineWeb + 30% synthetic memory tasks (Track A learning)
# ---------------------------------------------------------------------------
dataset_name: "HuggingFaceFW/fineweb"
dataset_config: "sample-10BT"
memory_task_ratio: 0.3

# ---------------------------------------------------------------------------
# Output
# ---------------------------------------------------------------------------
output_dir: "checkpoints/track_b/deltanet"
results_file: "results/track_b/deltanet.json"
log_interval: 50
save_interval: 1000

# ---------------------------------------------------------------------------
# Weights & Biases
# ---------------------------------------------------------------------------
no_wandb: false
wandb_project: "lrs-track-b"
wandb_run_name: "track-b-deltanet-qwen3.5-0.8b"
